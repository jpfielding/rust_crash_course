// arrays - fixed list where elems are the same types

pub fn run() {
    let numbers: [i32; 5] = [1, 2, 3, 4, 5];

    println!("{:?}", numbers);

    // get single va
    println!("{}", numbers[0]);
}